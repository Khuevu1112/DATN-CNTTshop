CREATE TABLE WARRANTY_HISTORY (

    id INT IDENTITY(1,1) PRIMARY KEY,

    request_id INT NOT NULL
        REFERENCES WARRANTY_REQUEST(id)
        ON DELETE CASCADE,

    status VARCHAR(30) NOT NULL,

    note NVARCHAR(1000),

    created_at DATETIME2 NOT NULL
        DEFAULT GETDATE()

);
GO
